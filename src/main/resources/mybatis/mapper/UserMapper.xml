<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.cachej.mapper.UserMapper">


    <sql id="Base_Column_List">
        id,username,age,mobile
    </sql>

    <resultMap id="BaseResultMap" type="com.example.cachej.domain.User">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="age" jdbcType="INTEGER" property="age"/>
        <result column="mobile" jdbcType="VARCHAR" property="mobile"/>
    </resultMap>

    <select id="getUser" parameterType="int" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_user where id = #{id,jdbcType=INTEGER}
    </select>

    <insert id="addUser" parameterType="com.example.cachej.domain.User">
        insert into t_user (username, age, mobile)
        values (#{username,jdbcType=VARCHAR}, #{age,jdbcType=INTEGER}, #{mobile,jdbcType=VARCHAR})
    </insert>


    <delete id="deleteUser" parameterType="java.lang.Integer">
        delete
        from t_user
        where id = #{id,jdbcType=INTEGER}
    </delete>

    <select id="getAllUsers" resultType="com.example.cachej.domain.User">
        select
        <include refid="Base_Column_List"/>
        from t_user
    </select>

    <update id="updateUser" parameterType="com.example.cachej.domain.User">
        update t_user
        <set>
            <if test="username != null">
                username = #{username},
            </if>
            <if test="age != null">
                age = #{age},
            </if>
            <if test="mobile != null">
                mobile = #{mobile}
            </if>
        </set>
        where id = #{id}
    </update>
</mapper>
